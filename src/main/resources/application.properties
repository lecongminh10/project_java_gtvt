# Server config
server.port=8081

# Application configuration
spring.application.name=project

# Database config (Oracle 19c)
# Common Oracle 19c service names: orcl, ORCLCDB, ORCLPDB1, XE
# Try one of these service names in the URL below
# spring.datasource.url=jdbc:oracle:thin:@localhost:1521:orcl
# spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# spring.datasource.username=mimi
# spring.datasource.password=Mimi123
# spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# MySQL config (dự phòng)
spring.datasource.url=jdbc:mysql://localhost:3306/project_gtvt?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=12345678
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# Disable automatic data.sql - using DataInitializationRunner instead
spring.sql.init.mode=never

# # Database config (MySQL) for docker
# spring.datasource.url=jdbc:mysql://localhost:3306/project_gtvt?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=12345678
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# Thymeleaf configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.example.project=DEBUG

# Mail (configure later)
spring.mail.host=smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=c0bda3008e7eb7
spring.mail.password=cdfe7180f5bf9c
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=no-reply@gtvt.edu.vn

# DevTools configuration - disable auto-restart to prevent classpath conflicts
spring.devtools.restart.enabled=false

# File upload configuration
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
